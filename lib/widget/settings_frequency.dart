import 'package:flutter/material.dart';

class SettingsFrequency extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('SettingsFrequency'),
    );
  }
}
